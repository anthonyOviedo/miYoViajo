{
  "name": "miyoviajo",
  "version": "1.0.0",
  "description": "Mapa de buses en Costa Rica",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:public": "VITE_EDITOR_MODE=false vite build --outDir dist-public",
    "build:admin": "VITE_EDITOR_MODE=true vite build --outDir dist-admin",
    "preview": "vite preview"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet.offline": "^3.2.1"
  },
  "devDependencies": {
    "vite": "^6.3.1",
    "wrangler": "^4.78.0"
  }
}
